# Pinned dependencies for running Wav2Lip on CPU (Intel Mac, Python 3.10).
# These versions are chosen to avoid the common `np.float` / librosa mel
# breakages that hit Wav2Lip on newer package releases.

torch==2.0.1
torchvision==0.15.2
numpy==1.23.5
scipy==1.10.1
librosa==0.9.2
numba==0.57.1
opencv-contrib-python==4.7.0.72
tqdm==4.66.1
